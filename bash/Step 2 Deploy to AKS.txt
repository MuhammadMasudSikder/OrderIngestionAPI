# kubectl apply -k deployments/overlays/dev
kubectl apply -k deployments/overlays/prod


az aks get-credentials --resource-group orderingestion-rg --name orderingestion-aks

Install Cert-Manager CRDs:
kubectl apply -f https://github.com/cert-manager/cert-manager/releases/latest/download/cert-manager.crds.yaml

Install Cert-Manager itself (example for Kubernetes 1.24+):
kubectl apply -f https://github.com/cert-manager/cert-manager/releases/latest/download/cert-manager.yaml

Verify:
kubectl get pods -n cert-manager
kubectl get clusterissuers
